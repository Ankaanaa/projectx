interface props {
  range: { value: { min: number | null; max: number | null }[]; label: string }
}

const FilterBlockRange = ({ range }: props) => {
  const
  return (
    <div>
      <div></div>
    </div>
  )
}
